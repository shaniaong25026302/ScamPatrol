---
all:
  children:
    production:
      hosts:
        scampatrol:
          ansible_host: REPLACE_WITH_EC2_PUBLIC_IP
          ansible_user: ubuntu
          ansible_ssh_private_key_file: REPLACE_WITH_PATH_TO_YOUR_PRIVATE_KEY

