# ── Scam Patrol environment (LOCAL ONLY — gitignored, never commit) ──

# App
PORT=3000
NODE_ENV=development

# MySQL — filess.io
DB_HOST=yy3xxq.h.filess.io
DB_PORT=3307
DB_USER=devops_readyfurof
DB_PASSWORD=a636c757e068042ec2ed9058142a52b30985fd14
DB_NAME=devops_readyfurof
# Filess.io free tier caps total connections at 5 (max_user_connections).
# Keep this low so a 2nd instance (e.g. Render + local) doesn't blow the cap.
DB_CONNECTION_LIMIT=3

# Auth — JWT signing secret (generated for this project)
JWT_SECRET=51c2aac8c1ea01237f2b945440ee0fec3d2d349aa27b6e0c56b4efb2b14fa7fec87bc7bf9dd17eb1772768fab4ec5862
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# AI Scam Checker — Google Gemini
GEMINI_API_KEY=AQ.Ab8RN6JOI_t5gjza04i9bMmrfsfpf_zcjz0hm2DAYalVa-Ea8A
GEMINI_MODEL=gemini-2.5-flash

# Email (password reset) — Gmail SMTP.
# SMTP_PASS is a Google "App Password" (16 chars, no spaces), NOT your Gmail login password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=shaniaong1216@gmail.com
SMTP_PASS=btrjonyrnxuivcze
MAIL_FROM=Scam Patrol <shaniaong1216@gmail.com>

# Email HTTP API — REQUIRED on Render (it blocks SMTP). When set, used instead of SMTP.
# Option A: Resend (no phone to sign up). Key starts with "re_". Free/no-domain sends
#   FROM onboarding@resend.dev and only TO your Resend account email (fine for a demo).
RESEND_API_KEY=
# RESEND_FROM=Scam Patrol <onboarding@resend.dev>
# Option B: Brevo (needs phone verification). Key starts with "xkeysib-".
BREVO_API_KEY=
# Option C: Mailjet (no phone). TWO keys from Account Settings → API Key Management.
# Verify MAIL_FROM's email as a Mailjet sender first.
MAILJET_API_KEY=31386f45d4cc3888d7fc868088054bba
MAILJET_SECRET_KEY=7ce08a516fea3e7f3e009f6c5e37dda0
# Public base URL used to build the password-reset link (local + Render share one DB,
# so tokens work from either — links always point to the live site).
APP_BASE_URL=https://scampatroll.onrender.com
