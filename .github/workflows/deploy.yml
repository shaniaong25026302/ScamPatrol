# <Shawn Start>
name: Deploy

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Verified image tag (e.g. sha-abc1234 or latest)"
        required: true
        type: string

  workflow_run:
    workflows:
      - CD
    types:
      - completed

permissions:
  contents: read
  packages: read

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    # Runs by hand always. Runs automatically after a SUCCESSFUL CD only when the
    # repository variable AUTO_DEPLOY is set to true.
    #
    # The switch matters for two reasons. workflow_run fires when CD finishes, including
    # when it finishes by failing, so the conclusion check is what stops a failed build
    # deploying itself. And an automatic deploy that cannot be turned off means nobody
    # chooses the moment: during a demonstration an unrelated merge would push to the live
    # host mid-presentation. AUTO_DEPLOY is currently true, so behaviour is unchanged; it
    # can be set to false to fall back to deploying by hand.
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' && vars.AUTO_DEPLOY == 'true')

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh

          printf "%s" "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

          ssh-keyscan -H "${{ secrets.EC2_HOST }}" >> ~/.ssh/known_hosts

      - name: Upload production Docker Compose
        run: |
          scp docker-compose.prod.yml \
            "${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/opt/scampatrol/docker-compose.prod.yml"

      - name: Determine Image Tag
        id: image
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "tag=${{ github.event.inputs.image_tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Deploy application
        env:
          IMAGE_TAG: ${{ steps.image.outputs.tag }}
        run: |
          ssh "${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}" "bash -s" << EOF
          set -e

          cd /opt/scampatrol

          echo "Logging into GHCR..."

          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io \
            -u "${{ github.actor }}" \
            --password-stdin

          CURRENT_IMAGE=\$(docker inspect scampatrol \
            --format='{{.Config.Image}}' 2>/dev/null || true)

          echo "Current image: \$CURRENT_IMAGE"

          IMAGE_NAME=\$(echo "ghcr.io/${{ github.repository }}:${IMAGE_TAG}" | tr '[:upper:]' '[:lower:]')

          export IMAGE_NAME

          echo "Deploying \$IMAGE_NAME"

          docker compose -f docker-compose.prod.yml pull

          docker compose -f docker-compose.prod.yml up -d

          echo "Verifying deployment..."

          sleep 15

          if docker compose -f docker-compose.prod.yml ps | grep -q "Up"; then
              echo "Deployment verified successfully."
              exit 0
          fi

          echo "Deployment verification failed."

          if [ -n "\$CURRENT_IMAGE" ]; then
              echo "Rolling back to \$CURRENT_IMAGE"

              docker pull "\$CURRENT_IMAGE"

              export IMAGE_NAME="\$CURRENT_IMAGE"

              docker compose -f docker-compose.prod.yml up -d

              echo "Rollback completed."
          else
              echo "No previous deployment found."
          fi

          exit 1
          EOF

      - name: Deployment Summary
        if: always()
        run: |
          {
            echo "## Deployment Summary"
            echo ""
            echo "- Image: ghcr.io/${{ github.repository }}:${{ steps.image.outputs.tag }}"
            echo "- Repository: ${{ github.repository }}"
            echo "- Server: ${{ secrets.EC2_HOST }}"
            echo "- Deployment workflow completed."
          } >> "$GITHUB_STEP_SUMMARY"
# <Shawn End>
